<% provide :title, t(".title") %>
<h1 class="page-header"><%= t ".achievements" %></h1>
<% if can? :manage, Achievement %>
  <%= render "admincp/sidebar" %>
<% end %>
<div class="container">
  <div class="row">
    <% if can? :manage, Achievement %>
      <div class="col-sm-9 col-md-9">
    <% end %>
      <div class="well">
        <ul class="users">
          <%= render @achievements %>
        </ul>
        <%= will_paginate %>
      </div>
    </div>
  </div>
</div>
